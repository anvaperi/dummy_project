$ git init
Initialized empty Git repository in C:/.../dummy_project/.git/

~/.../dummy_project (master)
$ git clone https://github.com/libgit2/libgit2

$ git [cmd] --help
$ git config --global user.email "[you@domain.xxx]"
$ git config --global user.name "[name]"
$ git config --global core.editor "vim"
$ vim ~/.gitconfig

I: Ignored
WD: Working directory
SA: Staging area
LR: Local Repository
RR: Remote Repository

add {WD -> SA}

commit {SA -> LR}
reset --soft [SHA] {LR -> SA}

commit -a {WD -> LR}
reset [--mixed] [SHA] {LR -> WD}

push {LR -> RR}
pull {RR -> LR}
fetch {RR -> LR}
checkout {LR -> WD}


$ git diff # compares 

$ git status
$ git status -s
$ git log
$ git log --oneline

.gitignore
  /[files]
  [folders]/

$ git add [file]
$ git rm --cached [file]

$ git commit -m "[titlgit e]"
$ git reset --soft HEAD^

$ git add -A && git commit
$ git reset HEAD^

$ [save file] && git add -A && git commit
$ git reset --hard HEAD^

$ git checkout #??
